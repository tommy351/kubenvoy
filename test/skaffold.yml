apiVersion: skaffold/v1beta6
kind: Config
build:
  artifacts:
    - image: tommy351/kubenvoy-test
      docker:
        dockerfile: test/Dockerfile
  local:
    useBuildkit: true
deploy:
  kubectl:
    manifests:
      - test/kubernetes/*.yml
